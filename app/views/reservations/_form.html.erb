<%= form_for(@reservation) do |f| %>
  <% if @reservation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reservation.errors.count, "error") %> prohibited this reservation from being saved:</h2>

      <ul>
      <% @reservation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :reservation_datetime %><br>
    <%= f.datetime_select :reservation_datetime %>
  </div>
  <div class="field">
    <%= f.label :reservation_date %><br>
    <%= f.date_select :reservation_date %>

  </div>
  <div class="field">
    <%= f.label :reservation_time %><br>
    <%= f.time_select :reservation_time, discard_minute: true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
